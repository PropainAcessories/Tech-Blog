{{> post-info post}}

{{#if loggedIn}}
<form class="comment-form">
    <div>
        <textarea class="ml-5" name="comment-body" id="comment-body"></textarea>
    </div>

    <div>
        <button class="button" type="submit">COMMENT</button>
    </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}

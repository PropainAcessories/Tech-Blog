<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Forum</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<div class="wrapper">
    <h1 class="has-background-dark has-text-white has-text-weight-bold is-size-1 has-text-centered">Tech Blog</h1>
    <nav class="navbar has-background-black-bis mt-2 has-text-justified">
      {{#if loggedIn}}
      <a href="/" class="navbar-item is-hoverable has-text-white pt-1">Home</a>
      <a href="/dashboard" class="navbar-item is-hoverable has-text-white pt-1">dashboard</a>
      <a href="/dashboard/new" class="navbar-item is-hoverable has-text-white pt-1">New Post</a>
      <button id="logout" class="button is-primary">Log Out</button>
      {{else}}
      <a href="/" class="navbar-item is-hoverable pt-1">Home</a>
      <a href="/login" class="button is-primary">Login</a>
      <a href="/signup" class="button is-primary">Create Account</a>
      {{/if}}
    </nav>

    <main>
        {{{ body }}}
    </main>


    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>